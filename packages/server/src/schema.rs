table! {
    users (id) {
        created_at -> Timestamp,
        email -> Varchar,
        first_name -> Varchar,
        id -> Int4,
        last_name -> Varchar,
        updated_at -> Timestamp,
    }
}
